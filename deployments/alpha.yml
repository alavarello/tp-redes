apiVersion: apps/v1
kind: Deployment
metadata:
  name: alpha
spec:
  replicas: 6
  selector:
    matchLabels:
      app: app-http
      version: alpha
  template:
    metadata:
      labels:
        app: app-http
        version: alpha
    spec:
      containers:
      - name: flask-api
        imagePullPolicy: Always
        image: alavarello/test-api:alpha
        ports:
        - containerPort: 8888
